# OpenArena on the A7A

Launcher with the same big.LITTLE core pinning and governor handling as the others.

    sudo cp openarena-gpu /usr/local/bin/ && sudo chmod +x /usr/local/bin/openarena-gpu
    openarena-gpu

Install the game itself from Debian: `sudo apt install openarena`
